export const rr = 10;
export const gap = 100;
export const padding = 5;
export const siblingPad = gap;
export const SHOW_LINKS_ONLY_ON_HOVER = true;
export const MAX_HOVER_LINKS = 300;

export const labelFontSizes = {
  zone: 14,
  environment: 13,
  tier: 12,
  application: 11,
  host: 10,
};

export const labelVisibility = {
  zone: true,
  environment: true,
  tier: true,
  application: true,
  host: true,
};
